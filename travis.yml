language: python # => 1
    python: # => 2
      - "3.8"
    services: # => 3
      - sqlite3
    env: # => 4
      -DJANGO=3.1.3
    install: # => 5
      - pip install -r requirements.txt
    script: # => 7
      - python manage.py test